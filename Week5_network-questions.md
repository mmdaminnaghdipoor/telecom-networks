network-questions

🧩 سوالات تستی (Multiple Choice)


۱. کدام‌یک از موارد زیر جزو اجزای اصلی شبکه مخابراتی نیست؟
A) گره (Node)
B) پیوند (Link)
C) کاربر (User)
D) ترانزیستور (Transistor)
✅ پاسخ: D — ترانزیستور جزء سخت‌افزار داخلی است نه جزء منطقی شبکه.


---

۲. کدام‌یک از روش‌های سوئیچینگ برای انتقال داده در شبکه‌های مدرن اینترنتی استفاده می‌شود؟
A) Circuit Switching
B) Message Switching
C) Packet Switching
D) Frequency Switching
✅ پاسخ: C — شبکه‌های اینترنتی مبتنی بر Packet Switching هستند.


---

۳. وظیفه‌ی لایه‌ی شبکه (Network Layer) در مدل OSI چیست؟
A) کنترل خطا و جریان داده
B) مسیریابی بسته‌ها بین مبدا و مقصد
C) فشرده‌سازی داده‌ها
D) نمایش داده به کاربر
✅ پاسخ: B — لایه شبکه مسئول مسیریابی و آدرس‌دهی است.


---

۴. پهنای باند (Bandwidth) در شبکه به چه معناست؟
A) سرعت CPU
B) ظرفیت کانال برای انتقال داده
C) تاخیر انتقال داده
D) تعداد کاربران شبکه
✅ پاسخ: B — پهنای باند بیانگر حداکثر نرخ انتقال داده است.


---

۵. کدام پروتکل در لایه انتقال (Transport Layer) استفاده می‌شود؟
A) IP
B) TCP
C) HTTP
D) ARP
✅ پاسخ: B — پروتکل TCP در لایه انتقال قرار دارد.


---

🧠 سوالات تشریحی (Descriptive)

۱. تفاوت بین Circuit Switching و Packet Switching را توضیح دهید.
پاسخ:
در Circuit Switching یک مسیر فیزیکی اختصاصی بین مبدا و مقصد ایجاد می‌شود (مثل تلفن سنتی). در Packet Switching داده‌ها به بسته‌هایی تقسیم می‌شوند و هر بسته ممکن است مسیر متفاوتی طی کند (مثل اینترنت). Packet Switching کارایی بالاتر و انعطاف‌پذیری بیشتری دارد.


---

۲. مدل OSI را توضیح دهید و نقش هر لایه را به اختصار بیان کنید.
پاسخ:
مدل OSI شامل ۷ لایه است:

1. Physical: انتقال بیت‌ها از طریق رسانه فیزیکی


2. Data Link: کنترل خطا و دسترسی به رسانه


3. Network: مسیریابی و آدرس‌دهی IP


4. Transport: کنترل جریان و اطمینان از تحویل داده


5. Session: مدیریت نشست‌ها و ارتباطات


6. Presentation: ترجمه، فشرده‌سازی و رمزنگاری داده


7. Application: خدمات مستقیم به کاربر (مثل HTTP, FTP)




---

۳. تفاوت بین IPv4 و IPv6 را بیان کنید.
پاسخ:
IPv4 دارای آدرس‌های ۳۲ بیتی است (حدود ۴ میلیارد آدرس) در حالی که IPv6 از آدرس‌های ۱۲۸ بیتی استفاده می‌کند و تقریباً تعداد نامحدودی آدرس فراهم می‌کند. IPv6 همچنین دارای ویژگی‌های امنیتی (IPSec) و پشتیبانی از پیکربندی خودکار است.


---

۴. منظور از تاخیر (Latency) در شبکه چیست و چه عواملی بر آن تأثیر دارند؟
پاسخ:
Latency مدت زمانی است که طول می‌کشد تا یک بسته از مبدا به مقصد برسد.
عوامل مؤثر شامل:

فاصله فیزیکی بین مبدا و مقصد

ازدحام شبکه (Congestion)

زمان پردازش در روترها

تأخیر صف (Queue Delay)



---

۵. مفهوم QoS (Quality of Service) را توضیح دهید و چرا اهمیت دارد؟
پاسخ:
QoS مجموعه‌ای از تکنیک‌هاست برای تضمین کیفیت سرویس در شبکه، مثلاً کنترل پهنای باند، کاهش تاخیر و جلوگیری از ازدحام. اهمیت آن در برنامه‌های حساس به تاخیر مثل تماس ویدئویی و VoIP بسیار بالاست.
